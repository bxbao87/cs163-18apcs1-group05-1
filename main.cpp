#include "Search.h"
#include "Support.h"
#include "History.h"
#include <map>

int main() {
	Search bogoSearch;
	bogoSearch.Run();
	/*Document doc("Data/Group25News41.txt");
	doc.ReadFile();
	std::vector<std::string> p = { "Google" };
	doc.GetParagraphForShowing(p);
	doc.debug();
*/
	/*if (IsStringWiths("Nintendo's"))
		std::cout << "True" << '\n';
	else
		std::cout << "False" << std::endl;*/
	return 0;
}