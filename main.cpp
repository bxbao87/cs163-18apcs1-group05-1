#include "Search.h"
#include "frontEnd.h"

int main()
{
	/*Search bogoSearch;
	bogoSearch.run();*/
	Trie trie;
	std::string tmp = "ahihi";
	std::string inFile = "nothing";
	/*trie.AddKey(tmp, inFile);
	tmp = "yolo";
	inFile = "something";
	trie.AddKey(tmp, inFile);*/
	//trie.SaveTrie("Process\\Index.txt");
	//FrontEnd();
	/*trie.LoadTrie("Process\\Index.txt");
	auto a = trie.GetKey(tmp);
	for (auto i : a)
		std::cout << i << ' ';*/

	system("pause");
	return 0;
}