#include "Search.h"
#include "Support.h"
#include "History.h"
#include <map>

int main()
{
	Search bogoSearch;
	//bogoSearch.Run();
	
	//std::string query("la la land AND (\"made in China\" OR intitle:something good");
	//std::cout << bogoSearch.InfixToPostfix(query);
	//FrontEnd();
	//bogoSearch.InputKey(28, 21);
	//auto a = bogoSearch.GetFilename("Data");
	//system("Start Data/1.txt");
	//NumIndex numL;
	//numL.LoadNumIndex();
	//std::vector<int> res;
	//double x = 30;
	////numL.GetNumKey(res,3);
	//numL.GetRange(res, 0.007, 0.015);
	/*if (!bogoSearch.SearchRange(0.012, 0.013, res))
		std::cout << "Not found" << '\n';
	for (auto i : res)*/
		//std::cout << i << ' ';
	

	system("pause");
	return 0;
}